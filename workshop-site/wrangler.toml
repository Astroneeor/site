# Cloudflare Workers Static Assets — SPA fallback (no _redirects needed).
# If your Worker has a different name in the dashboard, change `name` below to match.
name = "site"
compatibility_date = "2026-04-15"

[assets]
directory = "./dist/"
not_found_handling = "single-page-application"
